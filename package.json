{
  "name": "cts",
  "private": true,
  "packageManager": "pnpm@9.1.0",
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --handle-input --prefix '[{name}]' --names 'frontend,backend' \"pnpm --filter frontend run dev\" \"pnpm --filter backend run dev\""
  }
}
